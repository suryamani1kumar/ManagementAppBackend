export interface usersDetails {
  name: string;
  email: string;
  password: string;
  phone: number;
}

export interface dataDetail {
  series: string;
  match: string;
  time: string;
  invesment: number;
  returns: number;
  numberOfPlayer: number;
  created: string;
  comment: string;
  date: string;
}
